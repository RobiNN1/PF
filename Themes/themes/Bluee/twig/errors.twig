<div class="text">
    <div class="icon"><i class="fas fa-frown"></i></div>
    <h1>{{ info.status }}</h1>
    <p>{{ locale.errmsg }}</p>
    <a href="{{ info.back.link }}" class="btn btn-primary">{{ info.back.title }}</a>
</div>
